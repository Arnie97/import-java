[pytest]
addopts =
    --verbose
    --doctest-glob README.md
    --cov java
    --cov-append

[tox]
envlist = py3{4,5,6}

[testenv]
deps = cython
passenv = JAVA_HOME
commands = python setup.py pytest
